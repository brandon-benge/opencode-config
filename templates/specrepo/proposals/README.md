# Proposals

Spec reviewer agents create one subdirectory per request:

```text
specrepo/proposals/YYYY-MM-DD-short-name/
```

Each proposal should include an `architecture.md` file based on
`specrepo/templates/architecture-proposal.md`.

Proposals are drafts until a matching approval record exists under
`specrepo/approved/`.
