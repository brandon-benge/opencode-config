# Quality Spec

## Test Strategy

The default verification command is:

```bash
<test-command>
```

<Describe local test tools, mocking strategy, fixtures, and what must stay
deterministic.>

## Required Coverage Areas

- <Area>: <expected tests or checks>
- <Area>: <expected tests or checks>

## Compatibility Rules

- <Runtime, platform, API, schema, or config compatibility rule>
- <Runtime, platform, API, schema, or config compatibility rule>

## Security And Safety Rules

- <Secret-handling, network, filesystem, subprocess, or data safety rule>
- <Secret-handling, network, filesystem, subprocess, or data safety rule>

## Documentation Rules

User-visible behavior changes require corresponding documentation updates.
Architecture or workflow changes require corresponding updates under
`specrepo/specs/`.
