# Reusable Templates

This directory contains reusable template packs for opencode agents.

`specrepo/` is the template pack used by `@specrepo-bootstrapper` to create a
repo-specific SpecRepo structure in a target repository.
